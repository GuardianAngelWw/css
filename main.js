// Add your JavaScript code here
// You can use this file to handle interactivity and dynamic content

// Example: Add event listener to the group links
const groupLinks = document.querySelectorAll('.group-links a');

groupLinks.forEach(link => {
  link.addEventListener('click', () => {
    // Add logic to handle link click event
  });
});
